{
  "name": "monorepo-express-create-react-app",
  "version": "0.0.1",
  "private": true,
  "repository": "https://allardvanderouw@github.com/allardvanderouw/monorepo-express-create-react-app.git",
  "author": "Allard van der Ouw <allard.van.der.ouw@oliver-it.nl>",
  "license": "MIT",
  "scripts": {
    "start": "node workspace-server/server",
    "server:lint": "cd workspace-server && yarn lint",
    "server:test": "cd workspace-server && yarn test",
    "server:test:coverage": "cd workspace-server && yarn test:coverage",
    "server:test:report": "cd workspace-server && yarn test:report"
  },
  "workspaces": ["workspace-server", "workspace-client"],
  "nyc": {
    "exclude": [
      "**/test"
    ]
  }
}
